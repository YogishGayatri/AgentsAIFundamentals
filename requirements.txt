# Core — needed for every day of the course
langchain>=1.0
langgraph>=1.0
python-dotenv>=1.0
pydantic>=2.0

# Corporate networks: makes Python trust the OS certificate store, fixing
# "CERTIFICATE_VERIFY_FAILED self signed certificate" behind a TLS-intercepting
# proxy. Harmless on home networks. providers.py enables it automatically if present.
truststore>=0.9

# Providers — install at least the one you plan to use (Groq is the default)
langchain-groq>=0.3           # Groq
langchain-google-genai>=2.0   # Gemini      (optional)
langchain-openai>=0.3         # FM Gateway  (OpenAI-compatible, optional)
